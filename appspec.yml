version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "REPLACED_BY_CODEPIPELINE"
        LoadBalancerInfo:
          ContainerName: "bluegreen-task"
          ContainerPort: 8080

